version: '3.6'
services:
  rdd007:
    container_name: rdd007
    image: reddcoin-node:latest
    restart: always
    build: .
    networks:
      - default
    volumes:
      - "/usr/reddcoin:/root/.reddcoin"
    labels:
      guru.host: "wallet.shroomlife.de"
networks:
  default:
    external:
      name: shroom001